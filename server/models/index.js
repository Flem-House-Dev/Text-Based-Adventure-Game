const User = require('./User');
// const Character = require('./Character');
const Game = require('./Game');
const UserProgress = require('./UserProgress')

module.exports = { User, Game, UserProgress};
